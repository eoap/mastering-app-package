# otsu

Writes `otsu.tif` as a uint8 COG with 0 for non-water and 1 for water. The threshold is computed from finite values; nonfinite pixels are marked 0. An input with no finite values is rejected.

## Hatch setup

From the repository root:

```bash
cd water-bodies/command-line-tools/otsu
hatch env create default
source "$(hatch env find default)/bin/activate"
```

Python 3.10 or newer and Hatch are required. Run the command from the directory where outputs should be written:

```bash
otsu --raster norm_diff.tif
```

Run `deactivate` when finished. Progress and failures are logged by Loguru for the local processing tools.

## Checks and tests

From this package directory:

```bash
hatch check
hatch run test:test
```

The Ruff complexity limit is 4. Tests use local fixtures rather than remote imagery.

## Container

From the repository root:

```bash
podman build -t localhost:otsu:latest water-bodies/command-line-tools/otsu
```

The Dockerfile builds a wheel with Hatch and installs it in a non-root runtime environment.
The console command and the legacy `python /app/app.py` invocation are both available in the image.

## License

See [LICENSE.txt](LICENSE.txt).

## Generated CLI

`src/otsu/cli.py` is generated by `task code:bootstrap:otsu`. Keep custom logic in `otsu_impl.py`; Ruff excludes the generated file from linting and formatting.
